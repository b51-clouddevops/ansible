pipeline {
    agent any 
    environment {
        SSH_CRED = credentials('SSH-CRED')
    }
    stages {
        stage('Performing a DRY-RUN Role Validation'){
            steps {
                sh ""
            }
        }
    }
}