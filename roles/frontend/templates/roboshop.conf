    proxy_http_version 1.1;
    location /images/ {
        expires 5s;
        root   /usr/share/nginx/html;
        try_files $uri /images/placeholder.jpg;
    }

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;

    location /api/catalogue/ { proxy_pass http://catalogue-{{ENV}}.robot.internal:8080/; }

    location /api/user/ { proxy_pass http://user-{{ENV}}.robot.internal:8080/; }

    location /api/cart/ { proxy_pass http://cart-{{ENV}}.robot.internal:8080/; }

    location /api/shipping/ { proxy_pass http://shipping-{{ENV}}.robot.internal:8080/; }

    location /api/payment/ { proxy_pass http://payment-{{ENV}}.robot.internal:8080/; }

    location /nginx_status {
        stub_status on;
        access_log off;
    }

# This is a test commit